# import traceback
# import os
# from fastapi import HTTPException
# from .database import supabase

# def add_workflow(workflow):
#     """Add a workflow to the database."""
#     try:
        
#     except:
#         traceback.print_exc()
#         raise HTTPException(status_code=500, detail="Failed to add workflow to the database")